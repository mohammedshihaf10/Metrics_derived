# Path Coverage Tool Runner

This workspace mirrors the other technique folders, but for Python path-oriented exploration.

## Included Tool

- `crosshair`

## Note

This folder is Python-only. It uses `crosshair` against the existing branchy example code under `Cyclomatic Complexity/tool_examples`.
